name: build

on:
  push:

env:
  ARTIFACTS_PATH: artifacts
  PYTHON_VERSION: "3.12"
  CORE_BRANCH: "master"
  # Space-separated list of elements to install as dependencies
  ELEMENT_DEPS: "dbaas"
  PACKER_VERSION: "1.9.2"
  CORE_ENDPOINT_URL: http://10.20.0.2:11010
  CORE_USERNAME: "admin"
  CORE_PASSWORD: "admin"

jobs:
  Build:
    runs-on: [ self-hosted, vm ]
    strategy:
      fail-fast: true
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 30
      - name: Build Exordos S3
        env:
            PUSH_CFG: ${{ secrets.PUSH_CFG }}
        run: |
          set -eux

          mkdir -p ${ARTIFACTS_PATH}

          VERSION="$(exordos get-version .)"

          exordos build $(pwd)

          # Decode and write the push configuration
          echo "$PUSH_CFG" | base64 -d > exordos/exordos.push.yaml
          exordos push -c exordos/exordos.push.yaml -t local -f --latest

          # Temporary upload step is done in the build script
          echo "${VERSION}" > "${ARTIFACTS_PATH}/version.txt"
      - name: Upload the build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: build_artifacts
          path: artifacts/
  Func_tests:
    needs: Build
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: Setup `packer`
        uses: hashicorp/setup-packer@main
        with:
          version: ${{ env.PACKER_VERSION }}
      - name: Install exordos
        run: curl -fsSL https://repository.genesis-core.tech/install.sh | sudo sh
      - name: set exordos config
        run: |
          exordos settings set-realm default --endpoint $CORE_ENDPOINT_URL
          exordos settings set-context default --name default --user $CORE_USERNAME --password $CORE_PASSWORD
      - name: Install hypervisor
        run: sudo exordos compute hypervisors init
      - name: Bootstrap exordos_core
        run: exordos bootstrap -i latest -f -m core --admin-password $CORE_PASSWORD --cidr 10.20.0.0/22
      - name: sleep
        run: |
          sleep 25
      - name: Wait for Exordos Core API
        run: |
          for i in {1..60}; do
            if curl -s -o /dev/null -w "%{http_code}" http://10.20.0.2:11010 | grep -q "200\|401\|403"; then
              echo "Exordos Core API is ready"
              exit 0
            fi
            echo "Waiting for Exordos Core API... ($i/60)"
            sleep 5
          done
          echo "Exordos Core API did not become ready"
          exit 1
      - name: Set hypervisor with overbooking
        run: |
          exordos compute hypervisors add -u 63d6caf1-c452-4697-9d03-b4579612c1f5 -m VM --cores_ratio 10.0 --ram_ratio 10.0 -d driver=libvirt -d iface_mtu=1500 -d network_type=network -d network=exordos-core-net -d storage_pool=default -d connection_uri=qemu+tcp://10.20.0.1/system -d machine_prefix=dev-
      - name: Preload dependencies in background
        run: |
          for dep in $ELEMENT_DEPS; do
            exordos e e install $dep
          done
      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          name: build_artifacts
          path: artifacts/
      - name: install s3aas
        run: |
          VERSION=$(cat artifacts/version.txt)
          exordos e e install s3aas -v $VERSION
      - name: check exordos s3aas
        timeout-minutes: 5
        run: |
          set -eux
          
          while [ $(exordos ee l -o json -f name=s3aas | jq -r .[0]."Status") != "ACTIVE" ]; do
            sleep 5
          done
          exordos e e show s3aas
      # - name: Install requirements
      #   run: sudo apt install --yes libev-dev
      #- name: Set up Python ${{ matrix.python-version }}
      #  uses: actions/setup-python@v6
      #  with:
      #    python-version: ${{ matrix.python-version }}
      #- name: Install uv
      #  uses: astral-sh/setup-uv@v7
      #- name: Install tox-uv
      #  run: uv tool install tox --with tox-uv
      #      - name: Build exordos core
      #        run: |
      #          cd ../
      #          git clone -b "$CORE_BRANCH" https://github.com/exordos/exordos_core.git
      #          cd ./exordos_core
      #          export ALLOW_USER_PASSWD=true
      #          export FREQUENT_LOG_VACUUM=true
      #          exordos build -f . --manifest-var repository=https://repository.genesis-core.tech
      #          cd $GITHUB_WORKSPACE
      #- name: Build and start PaaS
      #  run: |
      #    #tox -e develop
      #    # TODO: to use .raw we need to patch manifest
      #    # export GEN_IMG_FORMAT_CORE=raw
      #    #.tox/develop/bin/python exordos_s3/tests/functional/prepare_env.py --project-dir . --output-dir /tmp/s3aas-build --endpoint http://10.20.0.2:11010 --username admin --password "${ADMIN_PASSWORD}"
      #- name: Find S3aaS CP IP
      #  run: |
      #    #sleep 6
      #    #exordos compute nodes list
      #    #S3AAS_CP_IP=$(exordos compute nodes list | grep s3aas-cp | awk -F'│' '{print $7}' | tr -d ' ')
      #    #echo "S3AAS_CP_IP=$S3AAS_CP_IP" >> $GITHUB_ENV
      #    #echo "Found S3aaS CP IP: $S3AAS_CP_IP"
      #- name: Wait for S3aaS CP API
      #  run: |
      #    #for i in {1..60}; do
      #    #  if curl -s -o /dev/null -w "%{http_code}" http://${S3AAS_CP_IP}:8080 | grep -q "200\|401\|403\|404"; then
      #    #    echo "S3aaS CP API is ready"
      #    #    exit 0
      #    #  fi
      #    #  echo "Waiting for S3aaS CP API... ($i/60)"
      #    #  sudo virsh list
      #    #  sudo ls -lah /var/lib/libvirt/images/
      #    #  sleep 10
      #    #done
      #    #echo "S3aaS CP API did not become ready"
      #    #exit 1
      #- name: Run functional tests
      #  env:
      #    EXORDOS_ENDPOINT: http://10.20.0.2:11010
      #    EXORDOS_USERNAME: admin
      #    EXORDOS_PASSWORD: ${{ env.ADMIN_PASSWORD }}
      #    EXORDOS_S3_CP_URL: http://${{ env.S3AAS_CP_IP }}:8080
      #    EXORDOS_POLL_TIMEOUT: 600
      #    EXORDOS_POLL_INTERVAL: 15
      #  run: |
      #    #tox -e ${PYTHON_VERSION}-functional
#      - name: Setup upterm session
#        uses: owenthereal/action-upterm@v1
#        if: ${{ failure() }}
#        with:
#          limit-access-to-actor: true
#          wait-timeout-minutes: 30
